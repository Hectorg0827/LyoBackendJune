# Pinned CPU image: Kokoro-82M behind an OpenAI-compatible speech endpoint.
# Keep this as a separate service so the main API image does not carry PyTorch.
FROM ghcr.io/remsky/kokoro-fastapi-cpu:v0.6.0
